import SwiftUI
import WidgetKit

/* The extension's entry point. One widget: the travel-day card. */
@main
struct TravelActivityBundle: WidgetBundle {
    var body: some Widget {
        TravelActivityWidget()
    }
}
